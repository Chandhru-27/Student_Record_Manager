# Student_Record_Manager
Student Record Manager: A console-based Python app with admin and student logins. Admins manage student profiles—add, update, delete—while students view report cards. Records include personal info, subject-wise marks, total, percentage, and GPA. Data persists in a JSON file with duplicate-checks and error handling.
